# Tattoy: Eye-candy for your terminal

Currently running with:

```
RUST_BACKTRACE=1 RUST_LOG="none,tattoy=trace" cargo run -- --use smokey_cursor
```

Testing with:

```
RUST_LOG="none,tattoy=trace" cargo test -- --nocapture
```

Logs go to: `./tattoy.log`
